extends layout 

block content 
    .container-fluid.shadow.d-flex.flex-column.p-3.col-sm-12.col-lg-6.text-center(style='width: 100%;height:70vh;')
        h1 Thanks #{contactName} for #{message1}.
        h4 #{message2}.
        if !login
            a.fs-3.mt-5.text-light-primary(href="/account") Login